## Commit c0bba04

本次提交将原先集中在 `main.cpp` 中的演示/流程逻辑大规模拆分并模块化，新增了完整的游戏主控类 `src/game/Game.h` 与 `src/game/Game.cpp`，让标题界面、难度选择、地图切换、主线任务、事件、战斗等流程由统一对象管理。与此同时，补齐了状态机骨架，新增 `src/state/ExplorationState.*`、`src/state/EventDialogState.*`、`src/state/CombatState.*`，把探索、事件对话、战斗从单文件流程中独立出来。

功能层面，本次提交还补全了多个关键系统：新增 `src/combat/CombatSystem.*` 负责情绪敌人生成与 d20 战斗结算；新增 `src/event/RandomEvent.*` 承担随机事件对象；新增 `src/fileio/FileManager.*`、`src/fileio/JsonFileManager.*`、`src/fileio/SaveManager.*`、`src/fileio/ConfigManager.*` 建立配置与存档读写基础；并继续扩展 `src/quest/QuestManager.cpp` 与本地化文本。整体上，这是一次“从 main.cpp 演示程序走向正式游戏框架”的重构提交，为后续真正的探索循环、状态切换与存档系统打下结构基础。
